[pytest]
pythonpath = src
testpaths = tests
addopts = -m "not slow"
markers =
    integration: tests that require local data/raw WFDB corpora
    slow: longer-running tests (opt-in with pytest -m slow)
